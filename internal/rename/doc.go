// Package rename collects filesystem entries under a root path and renames
// them according to the provided options, sorting deepest entries first to
// avoid renaming a parent before its children.
package rename
